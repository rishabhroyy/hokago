plugins {
    alias(libs.plugins.android.application)
    alias(libs.plugins.kotlin.android)
}

// Versions are injected by CI from the git tag; local/dev builds use
// gradle.properties defaults. (Named hokago* so they don't shadow the typed
// accessors Gradle generates from the gradle.properties keys themselves.)
val hokagoVersionName: String = providers.gradleProperty("appVersionName").getOrElse("0.2.0")
val hokagoVersionCode: Int = providers.gradleProperty("appVersionCode").getOrElse("1").toInt()

android {
    namespace = "com.hokago.app"
    compileSdk = 35

    defaultConfig {
        applicationId = "com.hokago.app"
        minSdk = 24
        targetSdk = 35
        versionCode = hokagoVersionCode
        versionName = hokagoVersionName
    }

    flavorDimensions += "form"
    productFlavors {
        phone {
            dimension = "form"
            manifestPlaceholders["isTv"] = "false"
        }
        tv {
            dimension = "form"
            manifestPlaceholders["isTv"] = "true"
        }
    }

    buildTypes {
        release {
            isMinifyEnabled = false
        }
    }

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }
    kotlinOptions {
        jvmTarget = "17"
    }
    buildFeatures {
        viewBinding = true
    }

    sourceSets {
        // Bundled SPA for offline mode lives under src/main/assets/web-dist
        // (CI copies apps/web/dist there; .gitkeep keeps the dir in the repo).
        getByName("main").assets.srcDirs("src/main/assets")
    }
}

dependencies {
    implementation(libs.androidx.core.ktx)
    implementation(libs.androidx.appcompat)
    implementation(libs.androidx.activity)
    implementation(libs.androidx.lifecycle)
    implementation(libs.androidx.leanback)
    implementation(libs.androidx.media)
}