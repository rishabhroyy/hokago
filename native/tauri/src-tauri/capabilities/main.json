{
  "$schema": "https://schema.tauri.app/config/2",
  "identifier": "default",
  "description": "The main window — remote origins and the bundled offline SPA may use core IPC (events) and the app's own commands.",
  "windows": ["main"],
  "remote": {
    "urls": ["http://**", "https://**", "hokago-spa://**", "hokago-file://**"]
  },
  "permissions": ["core:default"]
}